/**
 * ============================================================
 * api/index.ts — API 统一出口
 * ============================================================
 * 所有接口从这里统一导出，组件/composable 只从这里引入，
 * 不直接依赖具体文件，方便日后重构。
 * ============================================================
 */

export * from './dashboard-api'
export { default as http } from './http'
